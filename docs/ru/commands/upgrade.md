# ⏫ asc upgrade

Обновить приложение до новой версии: `<name>` (последняя) или `<name>@<version>`.

## Использование

```
asc upgrade <spec>
```

- **`spec`** — имя приложения, опционально с `@<version>` (позиционный, обязателен).

## См. также

- [📦 Пакетный менеджер](/ru/cli/package-manager)

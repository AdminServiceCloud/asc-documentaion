# ⏫ asc upgrade

Upgrade an app to a new version: `<name>` (latest) or `<name>@<version>`.

## Usage

```
asc upgrade <spec>
```

- **`spec`** — app name, optionally with `@<version>` (positional, required).

## See also

- [📦 Package manager](/cli/package-manager)

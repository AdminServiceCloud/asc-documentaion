# 💽 asc disk

Show disk usage — shorthand for [`asc app disk`](/commands/app#disk) (root
sees all users' apps).

## Usage

```
asc disk
```

A bar of total space occupied by apps against the store's filesystem
capacity, then each app's own usage, largest first.

## See also

- [📱 Application management](/cli/app-management)
